#include <stdio.h>
#include <stdarg.h>
/**
 */
void print_numbers(const char *separator, const unsigned int n, ...)
{
  va_list = list;
  va_start(list, n);
  if (*seperator == NULL)
    {
      continue;
    }
  for (i = 0; i < n; i++)
    {
      printf("%c", *separator);
    }
  printf("\n");
}
