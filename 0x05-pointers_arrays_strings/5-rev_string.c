#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *rev-string - returns a reversed string
 *@s: a character variable
 *Return: nothing
 */
void rev_string(char *s)
{
return (strrev(*s));
}
