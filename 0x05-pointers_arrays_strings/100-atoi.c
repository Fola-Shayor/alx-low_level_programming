#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *_atoi - converts a string to an integer
 *@s: a parameter
 *Return : integer
 */
int _atoi(char *s)
{
int i;
int conv;
for (i = 0; arr[i] != '\0'; ++i)
{
conv = conv * 10 + arr[i] - '0';
}
return (conv);
}
