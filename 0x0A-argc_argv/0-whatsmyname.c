#include <stdio.h>
#include "main.h"
/**
 *main - prints the name of the program
 *@argc: integer parameter
 *@argv: a string parameter
 *Return: 0 (success)
 */
int main(char *argv[])
{
printf("%s", argv[0]);
return (0);
}
