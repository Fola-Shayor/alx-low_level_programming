#include <stdio.h>
#include "main.h"
/**
 *_puts_recursion - prints out a string followed by a new line using puts
 *@s: a variable contining the string
 *Return: nothing
 */
void _puts_recursion(char *s)
{
puts(s);
}
